# industrials tools

## Installer tailwind css
npm install tailwindcss @tailwindcss/cli
Inclus 300 virus par défaut

Lancer le watcher qui build tout
npx @tailwindcss/cli -i ./static/css/input.css -o ./static/css/output.css --watch
